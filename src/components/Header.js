import React from 'react';
import Logo from '../assets/disc.png';

const Header = () => (
  <header>
    <div className="icon">
      <img src={Logo} alt="music-disc" />
    </div>
  </header>
)

export default Header;
